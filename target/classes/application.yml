server:
  port: 9090
  servlet:
    context-path: /community

spring:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML5

  datasource:
    # ���ݿ��������, ʹ��druid����Դ(Ĭ������Դ��HikariDataSource)
    type: com.alibaba.druid.pool.DruidDataSource
    #���ӳ�����
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/community?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT%2B8
      username: root
      password: root

      # ���ӳ����ã���С����С�����
      initial-size: 5
      min-idle: 5
      max-active: 20

      # ���ӵȴ���ʱʱ��
      max-wait: 30000

      # ���ü����ԹرյĿ������ӣ����ʱ��
      time-between-eviction-runs-millis: 60000

      # ���������ڳ��е���С����ʱ��
      min-evictable-idle-time-millis: 300000
  mail:
    host: smtp.163.com
    port: 465
    username: Xh321082561@163.com
    password: ZEYRBLDDVINQKDCR
    protocol: smtps
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          port: 465
          auth: true
          starttls:
            enable: true
            required: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
            port: 465
  redis:
    database: 11
    host: localhost
    port: 6379
  # Kafka
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: test-consumer-group
      enable-auto-commit: true
      auto-commit-interval: 3000
  # Quartz
  quartz:
    job-store-type: jdbc
    scheduler-name: communityScheduler
    properties:
      org:
        quartz:
          scheduler:
            instanceId: AUTO
          jobStore:
            class: org.quartz.impl.jdbcjobstore.JobStoreTX
            driverDelegateClass: org.quartz.impl.jdbcjobstore.StdJDBCDelegate
            isClustered: true
          threadPool:
            #一般使用这个便可
            class: org.quartz.simpl.SimpleThreadPool
            #线程数量，不会动态增加
            threadCount: 10
            threadPriority: 5

# MyBatis����
mybatis:
  # ���ñ���
  type-aliases-package: com.howe.community.pojo
  # ����XMLɨ���ַ
  mapper-locations: classpath:mapper/*.xml
  # ���������շ�������ʽ
  configuration:
    map-underscore-to-camel-case: true
    use-generated-keys: true

# community
community:
  path:
    domain: http://localhost:9090
    upload: e:/IdeaProject/upload

